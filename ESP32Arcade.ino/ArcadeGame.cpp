#include "Arduino.h"
#include "ArcadeGame.h"

// ========== ArcadeGame.cpp ==========
// Minimal “do nothing” implementation. Replace with real game logic.

void setupGame() {
  // Example: initialize your OLED/tft, frame buffers, input pins, etc.
  // pinMode(BUTTON_PIN, INPUT_PULLUP);
  // display.begin();
}

void loopGame() {
  // Example main loop: read buttons, update game state, draw frame.
  // bool up   = digitalRead(BUTTON_UP_PIN) == LOW;
  // bool down = digitalRead(BUTTON_DOWN_PIN) == LOW;
  // game.update(up, down);
  // game.draw();
}
