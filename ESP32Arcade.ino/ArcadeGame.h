#ifndef ARCADEGAME_H
#define ARCADEGAME_H

// ========== ArcadeGame.h ==========
// Minimal stubs for your game‐logic hooks. Replace these
// with your actual game‐initialization and per‐frame code.

/// Call this once at startup to initialize any displays, input pins, etc.
void setupGame();

/// Call this repeatedly from loop() to run your game loop (reading buttons, drawing frames, etc.)
void loopGame();

#endif // ARCADEGAME_H
